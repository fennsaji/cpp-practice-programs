#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int n;
    vector <int> arr;
    cin>>n;
    for(int arr_i=0;arr_i<n;arr_i++) {
        cin>>arr.push_back(arr_i);
    }
    for(int arr_i=0;arr_i<n;arr_i++) {
        
    }
    
    return 0;
}
